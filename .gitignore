.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.log
.env
.env.*
!.env.example
.vercel

# Local Cursor metadata (do not ship machine-specific paths)
.cursor/

# Next.js artifacts + synced wiki/widget mirror (never commit build output).
.next/
node_modules/

# Synced wiki mirror under public/ (generated by npm run sync-wiki). Track only the
# assistant bundle so production always has /chat-widget.js even if a host skips sync.
/public/*
!/public/chat-widget.js
